# General
alias la='lsd -la'
alias ls='lsd'
alias lst="lsd --tree --all"
alias md='mkdir'
alias rb='reboot'
alias sn='shutdown now'
alias vim='nvim'
alias dev='cd ~/Dev'

# Config Files
alias zshrc='cd ~/.dots/zsh/ && nvim .zshrc'
alias aliases='cd ~/.dots/zsh && nvim .zsh_aliases'
alias szsh='exec zsh'
alias nvimc='cd ~/.config/nvim/ && nvim init.lua'
alias waybarc='cd ~/.config/waybar/ && nvim config.jsonc'
alias swayc='cd ~/.config/sway/ && nvim config'
alias dots='cd ~/.dots/'
alias alac='cd ~/.config/alacritty/ && nvim alacritty.toml'

# Network Manager
alias wifi='nmtui'

# fzf
alias ff='cd ~ && ~/.local/bin/explorer.sh'
alias sd='cd ~ && cd $(fd --type d --hidden --exclude .git --exclude node_modules --exclude .cache --exclude .npm --exclude .mozilla --exclude go/pkg/ --exclude .cargo --exclude .rustup --exclude .nvm| fzf --height=70% --border=rounded )'

# File explorer
alias rn='ranger'

# Package Managers

# Pacman
alias pac='sudo pacman -S'
alias pacrs='sudo pacman -Rs'
alias pacu='sudo pacman -Syu'

# Yay
alias yayu='yay -Syu'
alias yayrs='yay -Rs'

# Git
alias ga='git add'
alias gb='git branch'
alias gbd='git branch -d'
alias gc='git commit'
alias gco='git checkout'
alias gcob='git checkout -b'
alias gd='git diff'
alias gl='git log'
alias glo='git log --pretty="oneline"'
alias gm='git merge'
alias gp='git push'
alias gpl='git pull'
alias gpo='git push origin'
alias gposu='git push origin --set-upstream'
alias grmr='git rm -r'
alias gs='git status'
alias gsw='git switch'
alias gswc='git switch -c'

# Lazygit
alias lg='lazygit'

# Docker
alias dcu='docker compose up'
alias dcd='docker compose down'

# Custom
alias q='exit'

# Umuzi
alias up='cd ~/Dev/work/projects'
alias su='cd ~/Dev/work/umuzi/stand-up/ && nvim .'

# Vite setup
alias vc='npm create vite@latest'
alias vcjs='cp -r ~/Dev/templates/vite-vanilla-js/ .'
alias vcts='cp -r ~/Dev/templates/vite-vanilla-ts/ .'

# NPM
alias nrd='npm run dev'
alias nrb='npm run build'
alias nt='npm test'

# PNPM
alias pncv='pnpm create vite'
alias pnrd='pnpm run dev'
alias pnrb='pnpm run build'

# P5.js
# alias cp5='gh repo create my-new-sketch --template=ezra-ramatong/p5js-starter --public'
alias p5='cp -r ~/Dev/templates/p5/ .'

# Ollama
alias chat="ollama run mistral"

# HTTP Server
alias pyserver="python3 -m http.server"

# Zellij
alias zl='zellij'

# Java
# --- Raw Java aliases ---

alias jjc='javac $JAVA_SRC_DIR/$JAVA_MAIN_CLASS.java'
alias jj='java -cp $JAVA_SRC_DIR $JAVA_MAIN_CLASS'
alias jjr='jjc && jj'

# --- Gradle project aliases ---
alias jgr='./gradlew run'
alias jgb='./gradlew build'
alias jgt='./gradlew test'

